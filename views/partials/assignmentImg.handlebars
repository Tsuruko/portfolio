<!doctype html>

<html>
	<div class="col-sm-2">
			<a class="thumbnail" data-toggle="modal" data-target="#myModalImg" onclick="popup(this)"
	data-imgSrc="{{image}}">
				<img src="{{image}}">
				<p>{{caption}}</p>
			</a>
	</div>
</html>